import { createSlice } from "@reduxjs/toolkit";
const initialState = {
  // user: null  fire basedan kelgan user saqlandi
  isAuthChange: false,

};


// action 
login:(state,{payload})=>{},
logaut:(state,{payload})=>{},
isAuthChange:(state,{payload})=>{},